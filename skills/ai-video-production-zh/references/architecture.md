# 架构边界

## Video Operation
创意、脚本、storyboard、hook、CTA、平台包装、SELECTED 决策。

## Shared Media
Promotion、Binding、TTS、Captions、Timeline、Render Contract、Artifact Inspect、Evidence、Usage Receipt、通用 QC。

## Blender / 3D Runtime
Shared Media 的视觉执行器，不是第二个视频系统。

## Remotion
最终 timeline/compositor：2D + 2.5D + 3D layers + UI + captions + audio + transitions → MP4。
