# AI_START_HERE — 下一次会话第一读

> 这是 `global-tool-radar` 的跨会话执行记忆入口。任何新的 AI/窗口在继续本项目之前，**先读本文件，再读取 GitHub 最新状态**。本文件记录的是执行约束和当前交接点；所有可变状态必须重新核验，不能把这里的 SHA/PR 状态当作永久事实。

## 1. 每轮固定启动顺序

1. 读取本文件。
2. 重新检查 `main` 最新 SHA、开放 PR、活跃分支、PR changed files / ownership，避免与其他窗口冲突。
3. 从 M1–M12 中选择**最早未完成且未阻塞**的业务步骤，当前优先级始终是：
   - M9 真成片 / 真素材闭环
   - M10 成片质量检查与真人批准
   - M11 抖音 / Bilibili 上传发布
   - M12 发布后真实数据反馈
4. 必须做出可验证的实际进展：代码、测试、workflow、receipt、commit、PR、merge、artifact 之一；不能只写计划。
5. 遇到必须真人登录、验证码、账号授权、人工审核、付款或其他敏感外部动作时，把所有可自动化前置做完，并明确指出**唯一真人阻塞点**。

## 2. 防跑偏规则

以下事情默认判定为“跑偏”，除非它们是解除当前 M9→M12 阻塞的直接必要条件：

- 新建与成片闭环无关的基础设施、抽象层、通用框架。
- 在已有可用路径时继续扩展新的 runner / orchestration / receipt 体系。
- 为了“以后可能有用”而新增与当前成片、质检、发布、数据回收无直接关系的代码。
- 把仓库里的生命周期逻辑、测试通过、preview、mock、receipt 当成真实发布或真实业务结果。
- 在真实成片仍未通过 M10 时提前推进 M11/M12 的“形式完成”。
- **把“技术上能播、教学上清楚”的 PPT/课件式视频误当成适合抖音/YouTube 的社交平台原生成片。**

如果本轮发现自己已经跑偏：

1. 在本文件的“跑偏记录”追加一条短记录；
2. 停止继续扩大该方向；
3. 回到当前最早未完成业务步骤。

## 3. 永久真实性边界

绝不伪造或暗示以下事实已经发生：

- 真实录屏 / 真人素材 / 真实旁白文件；
- 最终 MP4 已完成（除非有真实 MP4 + 加密/媒体证据）；
- 真人 M10 已审核 / 已批准；
- 抖音或 Bilibili 已登录、上传、发布；
- 平台内容 ID、公开 URL、发布时间；
- 平台 API 验证；
- 播放、点赞、评论、收藏、完播等真实指标；
- 本地 DeepSeek / 其他模型已实际运行（除非有真实运行证据）。

自动化证据只能证明它实际证明的内容。

## 4. 当前业务交接点（2026-08-07，继续前必须重验）

本文件本轮更新前最近一次已核验的 `main` 为 `471618aa0591afb56d3af3b98dea007751e5a079`，开放 PR 数量为 0；**本文件此次更新自身会再次推进 `main`，所以新会话必须重新查询最新 SHA/PR。**

当前真实业务状态：

- M9/M10 的可审看候选已经真实渲染出来，不再是 mock/preview。
- 已批准旁白：`zh-CN-XiaoxiaoNeural`。
- 批准 WAV SHA-256：`816d9ce2f2ea8090f1eb1940f30b7ad19e9d53ae388fbaa0346b063f2c45ac9e`。
- 批准 WAV 自然时长：`102.624s`。
- 时间线源码基线：`a6a02ff62f80422e7ce7a8581ec54fb2f1b80218`。
- 临时 render carrier PR `#85` 已完成任务并**关闭且未合并**；不要重开/合并它。
- 最终成功 workflow：`M10 Xiaoxiao Replacement Render v1`，run `31160564659`。
- 成功 artifact：`toolradar-m10-xiaoxiao-replacement-v1`，artifact ID `8986967862`。
- 最终候选文件：`toolradar-ai-design-xiaoxiao-final.mp4`。
- 最终候选 SHA-256：`9e1b394691d5e556d5d6df6cb731a5e7b9ccb17c040fc28cef3c8602f6e182fb`。
- 文件大小：`6,864,138` bytes。
- 视频：H.264，`1080x1920`，`30fps`，`3079` 帧，视频流时长 `102.633333s`。
- 音频：AAC 48kHz stereo，音频流/容器时长 `102.677333s`；相对批准 WAV 的编码/封装 padding 为 `+0.053333s`，已纳入有界技术校验。
- CJK 修复：GitHub Ubuntu render 明确安装并绑定 `Noto Sans CJK SC`。此前一版成片虽然完成 render，但代表帧里的中文为 tofu 方框，因此被拒绝进入真人 M10；修复后 8 个代表帧的中文均可正常辨认。
- 技术预检 PASS：精确帧数/尺寸/流完整性通过；`blackdetect >=0.5s` 为 0；`silencedetect -45dB >=1.0s` 为 0。

### 2026-08-07 真人业务判断（最高优先级）

用户对这版的明确判断是：

- **声音基本没问题。**
- **PPT/演示本身基本没问题。**
- 这种形态很适合教材、课程、培训、讲解类内容，可以直接外化/复用到 `Training OS` 的视频制作能力。
- **但作为“视频运营公司/平台”要投放到抖音、YouTube 等社交内容平台，这种 PPT/课件式形态不合格。**
- 原因不是编码、字体、旁白或 Remotion 技术失败，而是**内容形态和平台消费场景不匹配**：它更像培训教材，不像会让普通用户停留、继续看、互动和传播的社交原生视频。
- 因此：**不要把 SHA `9e1b3946...` 这支视频推进 M11 发布。** 它可以作为 Training OS 教学视频模板/能力证明保留，但不作为 ToolRadar 社交运营内容发布候选。

当前真实性状态：

- **`technicalMediaIntegrityPassed=true`**。
- **`instructionalPresentationFit=accepted_by_human`**（仅表示用户认为声音+PPT形态用于教材/培训基本没问题）。
- **`socialPlatformBusinessFit=rejected_by_human`**。
- **`humanQualityApprovedForSocialPublication=false`**。
- **`publicationAllowed=false`**。
- **`publicationPerformed=false`**。
- **`analyticsObserved=false`**。

### 当前真正业务阻塞

不是再修 PPT，也不是立刻登录发布；而是先产出一支**社交平台原生**的真实候选片，再做 M10 真人业务质检。

下一支候选必须明显区别于教材/PPT模式，至少按以下思路设计：

1. 前 1–3 秒必须有强 hook：反常识、冲突、结果先给、问题、悬念或强视觉动作，不能从“今天我们来介绍……”开始。
2. 画面不能长期停留在整页 PPT；需要人物/角色、真实/仿真场景、产品实操、动态素材、B-roll、特写、视觉反差、字幕强调等多层画面来源。
3. 节奏按社交短视频设计：频繁的信息状态变化、镜头变化、构图变化、字幕变化、音效/音乐变化，而不是每十几秒换一张课件。
4. 文案以“让人继续看”为第一目标，而不是“完整讲清楚”为第一目标；先制造信息缺口，再逐步兑现。
5. 每条内容应有明确情绪/娱乐/欲望驱动之一，例如：惊讶、好奇、爽感、失败反转、比较、挑战、争议、实测、前后对比、结果揭晓。
6. 技术/工具类内容也要“内容化”：实测一个结果、挑战一个任务、对比两个方案、踩坑/翻车/逆转，而不是纯功能介绍。
7. 在进入 M11 前，人必须回答的不只是“声音和画面有没有问题”，还要回答：**“我如果刷到它，会不会停下来？会不会继续看？有没有理由转发/评论/收藏？”**

## 5. 两类视频能力必须分开

### A. Training OS / 教材培训模式

现有 `Remotion + 晓晓旁白 + PPT/界面演示` 路线是有效资产，可以直接复用：

- 课程讲解
- 教材视频
- 培训平台章节
- SOP/产品说明
- 内部知识培训
- 屏幕演示/结构化教学

这条路线目标是：**清楚、稳定、可控、可复用、低成本批量生产。**

### B. ToolRadar / 社交平台运营模式

需要单独的创作语言，目标是：**停留、完播、互动、传播、转化。**

不能只是把 Training OS 模板加快、加动画、加音乐；必须从选题、脚本、hook、叙事、画面素材和剪辑节奏层面重新设计。

## 6. 当前主目标

ToolRadar 当前主目标更新为：

**保留已验证的 Training OS 教学视频能力 → 不发布当前 PPT 候选 → 研究/定义社交平台原生内容形态 → 生成第一支真正 social-native 候选 → M10 真人判断“会不会停留/继续看/互动” → 通过后才进入 M11 真发布 → M12 回收真实数据。**

任何新工作都先问：它是在提升“平台原生内容竞争力”，还是只是在继续优化课件？如果只是后者，默认不要在 ToolRadar 主线上继续做。

## 7. 跑偏记录 / 复盘记忆

- 2026-08-07：项目曾多次向 runner/orchestration/receipt 基础设施扩张。此后新增基础设施必须证明是解除当前成片闭环阻塞的直接必要条件；否则停止并回到 M9/M10/M11/M12。
- 2026-08-07：用户明确要求“跑偏的事情写下来，下次首先读”。本文件即为固定入口。
- 2026-08-07：M10 的第一次技术 render 已成功，但代表帧检查发现 Ubuntu 缺中文字形，画面中文变成 tofu 方框。以后在要求真人审完整成片之前，必须先做真实输出的可视帧/文字检查；不能只看 render/ffprobe 是否成功。
- 2026-08-07：旧 M10 preflight 曾把 AAC 封装产生的约 `53ms` 尾部 padding 误判为时长失败。技术门应以精确帧数/视频时间线为主，并对编码器可解释的小幅 padding 使用有界校验，不能用脆弱的容器总时长硬阈值。
- 2026-08-07：**最重要的新复盘：技术可播 ≠ 教学可用 ≠ 社交平台可运营。** 当前 PPT+旁白成片在教材/Training OS 场景成立，但在抖音/YouTube 运营场景被真人明确拒绝。以后 M10 必须包含 `socialPlatformBusinessFit`，不能再只做音画技术质检。

## 8. 每轮结束必须留下的证据

最终汇报只用以下五个标题：

- `本轮实际完成`
- `验证证据`
- `最新主线 SHA`
- `整体进度变化`
- `下一阻塞点`

另外，每轮生成一个新的、可点击的独立 HTML 进度文件放在 `/mnt/data`，并在最终回复中给出链接。
